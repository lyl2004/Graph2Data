# CSV 校正模块理解与设计记录

本文档记录当前对 `src` 下 CSV 校正工具的理解，以及后续与本地图表提取链路衔接时建议保留的设计边界。

## 当前定位

当前 CSV 校正工具的输入主要面向 Automeris / WebPlotDigitizer 等外部工具导出的曲线点数据。它并不直接负责从图片中识别曲线，而是接收已经提取出来的二维点，完成坐标映射、异常点处理、拟合和导出。

后续项目工作中心转向 `tests` 下尚未成型的本地提取工具后，CSV 校正模块仍应作为稳定的后处理内核存在。理想情况下，本地提取工具输出统一的归一化点或像素点，CSV 校正模块负责把它们转换为可检查、可拟合、可导出的真实数据。

## 现有处理流程

当前核心入口是 `csv_processor.process_pipeline(params)`，其流程为：

1. 读取 CSV 前两列为 `raw_x` 和 `raw_y`。
2. 将输入点视为归一化坐标。
3. 可选执行四角仿射校正。
4. 如果未执行角点校正，则按参数选择越界处理：
   - 允许外推：保留超出 0-1 的归一化值。
   - 裁剪至边界：把 X/Y 分别裁剪到 0-1。
   - 重新归一化：按当前点集的 min/max 重新压缩到 0-1。
5. 根据用户输入的物理坐标范围，把归一化坐标映射到实际坐标。
6. 按 X 坐标排序。
7. 可选执行离群点清洗。
8. 可选执行曲线拟合或插值。
9. 返回原始校正点、拟合点、置信区间、删除点数和角点校正状态。

GUI 入口是 `csv_gui.py`，负责上传 CSV、收集参数、调用 `process_pipeline`、绘制 Plotly 图并导出 corrected CSV。

## 关键设计假设

当前实现默认有几个重要假设：

- 输入 CSV 的前两列可以解析为浮点数。
- 输入点是单条曲线。
- 输入点可以被看作 `x -> y` 的函数关系。
- 坐标映射前的点大多接近 0-1 归一化坐标。
- 拟合前按 X 排序是合理的。
- 四角校正依赖点集中存在接近包围盒四角的点。

这些假设对 WebPlotDigitizer 导出的单条曲线结果通常可接受，但对后续本地自动提取链路不一定充分。尤其是多曲线、曲线回折、重复 X、局部遮挡补全、像素坐标输入等场景，需要更明确的数据契约。

## 建议保留的职责边界

CSV 校正模块建议长期承担以下职责：

- 统一读取外部提取和本地提取结果。
- 把归一化坐标或像素坐标映射为物理坐标。
- 提供异常点、越界点、重复点和拟合质量诊断。
- 输出标准化数据文件。
- 为本地提取算法提供 benchmark 和质量验收。

CSV 校正模块不建议承担以下职责：

- 图像坐标轴检测。
- OCR 识别刻度或标签。
- 曲线颜色分割。
- 曲线像素骨架追踪。
- 遮挡曲线补全。

这些能力应由本地提取模块实现，然后把结果交给 CSV 校正模块。

## 后续优化方向

1. 输入读取层

   新增独立的 `load_points()`，支持有表头、无表头、逗号/制表符分隔、多 dataset、异常行报告。不要继续静默跳过所有异常。

2. 数据质量报告

   每次处理返回结构化诊断信息，包括：
   - 原始行数和有效点数。
   - 异常行数。
   - 超界点比例。
   - 重复 X 数量。
   - X 是否单调。
   - 清洗删除点数。
   - 拟合残差指标。
   - 角点校正是否可靠。

3. 多曲线支持

   为后续本地提取工具准备多曲线输入输出格式。建议支持 `curve_id, x_raw, y_raw, x_norm, y_norm, x_corrected, y_corrected, flag` 这样的长表结构。

4. 输入坐标模式

   明确区分：
   - WebPlotDigitizer 已校准物理坐标。
   - WebPlotDigitizer 或本地工具输出的归一化坐标。
   - 本地工具输出的像素坐标。

   不同模式应使用不同映射策略，避免对已经是物理坐标的数据重复映射。

5. 拟合前预处理

   增加重复 X 合并、局部点密度均衡、非单值曲线检测、回折曲线提示等处理。

6. Benchmark

   将 `csv_create.py` 中的理论曲线校验思路正式化，用固定测试图和理论数据持续比较 WebPlotDigitizer、本地提取、校正后结果的 RMSE、MAE、Max Error 和 R2。

## 与本地提取模块的接口设想

本地提取模块最终可输出如下结构：

```text
curve_id,x_pixel,y_pixel,x_norm,y_norm,color_id,confidence,source_flag
```

CSV 校正模块接收该结构后：

1. 按 `curve_id` 分组。
2. 根据坐标轴检测结果或用户输入范围完成映射。
3. 对每条曲线独立清洗和拟合。
4. 生成统一质量报告。
5. 导出长表 CSV、分曲线 CSV 或 Excel。

这样设计可以让后续本地提取算法逐步替代网站导出结果，同时不破坏现有 CSV 校正工具的价值。
